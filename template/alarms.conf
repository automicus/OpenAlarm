<alarms>

	<alarm>
		<name></name>
		<action>http://[server]:[port]/[path]</action>
        <off>http://[server]:[port]/[path]</off> <!-- This line is optional -->
        <snooze>http://[server]:[port]/[path]</off> <!-- This line is optinal -->
	</alarm>

    <!-- the above block can be repeated as needed -->

</alarms>
